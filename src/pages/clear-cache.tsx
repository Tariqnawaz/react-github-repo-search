import ClearCacheContainer from '../container/clearCacheContainer'

/**
 * ClearCache functional component is a that clear the cache data.
 * ClearCacheContainer: is a redux container which maps the state & action for clearing cache.
 * 
 */
const ClearCache = () => {
    return (
        <ClearCacheContainer/>
    );
}

export default ClearCache;